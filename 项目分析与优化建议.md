# IPTV Proxy 项目分析与优化建议

> 分析对象：`iptv-proxy`（基于 Cloudflare Pingora 的 IPTV 透明代理，面向 OpenWrt IPQ60xx / aarch64）
> 分析范围：`src/main.rs`、`Cargo.toml`、`README.md`、部署脚本、CI 流程

---

## 一、项目理解

### 它是什么
一个用 Rust + Pingora 写的**单二进制 HTTP 反向代理**，专门给 IPTV 播放器用。核心目的是把上游返回的 `m3u8` 播放列表"改写"一遍，让里面的媒体分片（`.ts` 等）也走本机代理，从而解决跨域、防盗链 Referer、以及某些源把 `.ts` 伪装成 `.jpeg` 下发的问题。

### 实际工作流（与 README 不符，见第三节）
1. 客户端请求 `http://<ip>:8080/?url=<percent-encoded 目标 URL>`。
2. `request_filter` 解析 `url=`，记录目标 URL、判断是否 m3u8、是否需 jpeg 修正，并改写本机请求路径。
3. `upstream_peer` 建立到目标主机的连接（HTTP/HTTPS 自动识别）。
4. `upstream_request_filter` 注入 Host / User-Agent / Referer / 指定转发头，m3u8 时摘掉 `Accept-Encoding`。
5. `response_filter` 改写 3xx 跳转的 `Location`、处理 jpeg 修正、m3u8 时摘掉 `Content-Length`。
6. `response_body_filter` 对 m3u8 正文逐行改写：绝对 URL 和相对媒体路径都被改写成 `http://<local_ip>:<port>/?url=<encoded>`。

### 架构
- 单 `http_proxy_service`，实现 `ProxyHttp` trait。
- `ProxyConfig`（local_ip / bind_port）+ `ProxyContext`（每次请求状态）。
- 静态链接 musl，交叉编译到 `aarch64-unknown-linux-musl`，由 OpenWrt `procd` 守护。
- CI 用 `messense/rust-musl-cross` 容器自动出包并打 tar.gz / Release。

---

## 二、必须优先修复的问题（高优先级）

### 1. 分块（chunked）m3u8 改写存在正确性 Bug
`response_body_filter` 每次收到一个 body chunk 就独立调用，`pending_tag` 是局部变量、不跨 chunk 保存。更严重的是：

- **URI 被 chunk 边界截断**：若一行 `http://.../segment001.ts` 在两 chunk 之间断开，前半段被当成完整行改写（截断的错误 URL），后半段 `.ts` 后缀又被当媒体资源二次改写 → 播放列表损坏。
- **多字节 UTF-8 跨 chunk 截断**：`std::str::from_utf8(bytes)` 失败直接 `return Ok(None)`，该 chunk 原样透传 → 包含未改写（甚至半截）的 URL。

**修复方向**：把"未处理完的半行"和待刷新的 `pending_tag` 存进 `ProxyContext`，跨 chunk 累积；UTF-8 用字节缓冲直到可安全解码。

```rust
// ProxyContext 新增字段
pub struct ProxyContext {
    // ...原有字段
    pending_tag: Option<String>,   // 跨 chunk 保存
    line_buf: Vec<u8>,             // 半行缓冲（含跨 chunk 的字节）
}
```

### 2. 开放代理 / SSRF 风险（安全）
任何能访问 `0.0.0.0:8080` 的人都能让它请求**任意内网或公网地址**（`/?url=http://169.254.169.254/...`、`/?url=http://内网管理页`）。代码里**没有任何目标主机白名单**。

- `deploy-openwrt.sh` 与 CI 的 init 脚本都写死 `BIND_ADDR="0.0.0.0:8080"`，README 却声称"仅绑定内网 IP"。
- `README` 的"安全建议"也只说"用防火墙"，代码层面无防护。

**修复方向**：增加目标域名/IP 白名单（环境变量 `ALLOW_HOSTS`），默认拒绝非白名单；或至少默认绑定到具体 LAN 接口而非 `0.0.0.0`。

### 3. 文档与代码严重不一致（影响可用性）
`README.md` 与 CI 生成的包内 `README.md` 描述的行为**全是旧版本**：
- 文档说 URL 格式是 `/iptv/http://...` 和 `/proxy/host:port/path` —— 实际代码只认 `/?url=`。
- 文档说"自动识别 surrit.com / fourhoi.com 注入 Referer"、"`116.199.x.x` 自动改写" —— 代码里**完全没有**这些域名特判，而是对所有缺 Referer 的请求统一注入写死的 `DEFAULT_REFERER`。
- 文档说 m3u8 改写为 `/proxy/116.199...` —— 实际改写为 `/?url=...`。

新用户照文档用必然 400 / 不生效。

---

## 三、依赖与构建（中优先级）

### 4. 6 个依赖完全没用到（死依赖）
已在 `src/` 全文 grep 确认：`reqwest`、`memchr`、`regex`、`once_cell`、`ahash`、`num_cpus` **在代码中从未被引用**。

后果：
- 编译时间更长、二进制更大、攻击面更大。
- `README` 宣称"SIMD 加速：使用 memchr 进行字符串搜索（2-3x 提速）"——**memchr 根本没调用**，属虚假性能声明。

**建议**：从 `Cargo.toml` 删除这 6 项；如后续确实需要字符串搜索，再显式引入 `memchr` 并真正使用。

### 5. 缺少 `Cargo.lock`，构建不可复现
项目无 `Cargo.lock`，而 `pingora` 直接锁某个 git `rev`（`d9e6d7a`）。虽然 rev 固定，但其余依赖（reqwest、tokio 等传递依赖）**浮版本**会随构建时间漂移，CI 出的包在不同日期可能行为不同。

**建议**：提交 `Cargo.lock`（即便用 git 依赖也能锁传递依赖版本）。

### 6. `WORKERS` 环境变量是空操作
`deploy-openwrt.sh`、CI init 脚本、README 都设置 `WORKERS="4"`，但 Rust 代码**从不读取**该变量。Pingora `Server::new` 默认按 CPU 核数起线程，配置项等于没用。

**建议**：要么在代码里用 `Opt::threads` 真正读取并应用 `WORKERS`，要么从文档/脚本里删掉，避免误导。

---

## 四、性能（中优先级）

### 7. 每请求 `info!` 日志在高并发下成为瓶颈
`upstream_peer`、`upstream_request_filter`、`response_body_filter`、`logging` 都在**热路径**打 `info!`：
- `info!("Upstream: {}:{} ...")`
- `info!("Upstream request headers -> UA: {}, Referer: {}")`
- `info!("M3U8 rewritten ({} lines)")`

实测目标 2000+ req/s，仅这几条就会每秒产生 8000+ 日志行，日志加锁会明显拖慢吞吐、冲刷磁盘。

**建议**：热路径日志降到 `debug!`/`trace!`；只有错误和启动信息用 `info!`/`error!`。

### 8. m3u8 改写逐行 `urlencoding::encode` 反复分配
`response_body_filter` 对每行 `String` 拼接 + `encode`（每次都新分配 `String`）。对超长播放列表（数千分片）会有可见分配压力。可在确认是 m3u8 后仅对媒体行编码，并复用 `urlencoding` 结果缓冲。属于微优化，仅当你遇到超大列表时做。

---

## 五、健壮性与可维护性（中/低优先级）

### 9. 改写 URL 写死 `http://` 与 `local_ip`，不支持域名/HTTPS
`m3u8` 改写一律生成 `http://{local_ip}:{port}/?url=`。若通过域名、反代 HTTPS、或非 `local_ip` 的地址访问代理，生成的播放列表 URL 会失效。

**建议**：从入站请求的 `Host` 头（含 scheme）推导"本代理基址"，而不是用配置里的 `local_ip`。

```rust
// 用客户端 Host 推导，而不是 self.config.local_ip
let host = session.req_header().headers.get("host")
    .and_then(|v| v.to_str().ok())
    .unwrap_or(&self.config.local_ip);
let scheme = /* 由前端协议或配置决定，默认 http */;
let proxy_base = format!("{}://{}", scheme, host);
```

### 10. `is_likely_media_resource` 的"纯数字文件名"例外可能漏改
```rust
if filename.is_empty() || filename.chars().all(|c| c.is_ascii_digit()) {
    return false;
}
```
像 `/00000000/12345.ts` 这种（目录是数字、文件名也是纯数字）会被**误判为非媒体**而跳过改写。实践中 IPTV 段名常是纯数字。

**建议**：对 `starts_with('/')` 的**纯路径**直接判定为媒体（已经是），对带主机名的相对路径再走扩展名 + 排除逻辑；或移除纯数字例外。

### 11. `BIND_ADDR` 解析脆弱
```rust
let bind_port: u16 = bind_addr.split(':').nth(1)...
```
对 IPv6（`[::1]:8080`）会解析失败。建议用 `std::net::SocketAddr` 解析。

### 12. 写死的 `DEFAULT_REFERER` / `DEFAULT_USER_AGENT` 指向特定站点
`DEFAULT_REFERER = "https://missav.ws/dm242/cn"` 看起来是某个具体片源的防盗链 Referer，对所有缺 Referer 的请求统一注入。这既不可配置，也有隐私/泄露隐患（把你的访问来源固定成某站）。

**建议**：改为可配置环境变量（如 `DEFAULT_REFERER`），默认值留空或通用；文档注明用途。

### 13. `deploy.sh` 存在明文密码与硬编码路径
`deploy.sh` 里 `sshpass -p '123456tyui'` 明文密码、`cd /home/tetora/Desktop/...` 绝对路径，且与 `deploy-openwrt.sh` 是**两套不同目标/不同机制**（一个 systemd、一个 procd），容易混淆。

**建议**：统一部署入口；密码用环境变量/SSH key，不要落盘；去掉个人绝对路径。

---

## 六、优化优先级清单

| 优先级 | 项 | 类型 | 工作量 |
|--------|----|------|--------|
| 🔴 高 | 1. 分块 m3u8 改写 + UTF-8 跨 chunk 缓冲 | 正确性 | 中 |
| 🔴 高 | 2. 目标主机白名单 / 防 SSRF | 安全 | 小 |
| 🔴 高 | 3. README 与代码对齐（重写文档/包内 README） | 可用性 | 小 |
| 🟠 中 | 4. 删除 6 个死依赖 | 构建/体积 | 极小 |
| 🟠 中 | 5. 提交 `Cargo.lock` | 可复现 | 极小 |
| 🟠 中 | 6. 真正支持 `WORKERS` 或删除该配置 | 一致性 | 小 |
| 🟠 中 | 7. 热路径日志降级到 debug/trace | 性能 | 极小 |
| 🟠 中 | 9. 用请求 Host 推导代理基址（支持域名/HTTPS） | 健壮性 | 小 |
| 🟡 低 | 8. m3u8 改写分配优化 | 性能 | 小 |
| 🟡 低 | 10. 修正纯数字段名误判 | 正确性 | 极小 |
| 🟡 低 | 11. `BIND_ADDR` 用 `SocketAddr` 解析 | 健壮性 | 极小 |
| 🟡 低 | 12. Referer/UA 改为可配置 | 可维护性 | 小 |
| 🟡 低 | 13. 统一部署脚本、去除明文密码 | 工程化 | 小 |

---

## 七、快速见效的最小改动建议
如果只想先动低风险的几处，按收益/风险比排序：
1. **删 6 个死依赖**（改 `Cargo.toml` 一行）——编译更快、体积更小、消除虚假声明。
2. **提交 `Cargo.lock`** ——一条命令，构建可复现。
3. **热路径 `info!` → `debug!`** ——高并发下直接提升吞吐。
4. **重写 README 的 URL 格式与功能描述** ——让新人能用起来。
5. **加 `ALLOW_HOSTS` 白名单 + 默认不绑 `0.0.0.0`** ——堵住最大的安全隐患。
